
;;
;; Helper routines for jumping via vectors, to be used with JSR
;;

via_IOPEN:
	jmp (IOPEN)

via_ICLOSE:
	jmp (ICLOSE)

via_ICHKIN:
	jmp (ICHKIN)

via_ICKOUT:
	jmp (ICKOUT)

via_ICLRCH:
	jmp (ICLRCH)

via_IBASIN:
	jmp (IBASIN)

via_IBSOUT:
	jmp (IBSOUT)
	
via_ISTOP:
	jmp (ISTOP)
	
via_IGETIN:
	jmp (IGETIN)
	
via_ICLALL:
	jmp (ICLALL)
	
via_ILOAD:
	jmp (ILOAD)
	
via_ISAVE:
	jmp (ISAVE)
